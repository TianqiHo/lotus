package com.lotus.core.encryption;
/*
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.SpringBootConfiguration;

import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;

@SpringBootConfiguration
public class Encryption {
	
	@Autowired
	private BCryptPasswordEncoder bCryptPasswordEncoder;
	
	public String encryption(String rawPassword) {
		return bCryptPasswordEncoder.encode(rawPassword);
	}
	
	public boolean matches(String rawPassword,String encryptionPassword) {
		return bCryptPasswordEncoder.matches(rawPassword, encryptionPassword);
	}
}*/
